# Dependencies
node_modules/

# Build output
dist/
build/
.wrangler/

# Environment / secrets — ห้าม commit ค่าจริงเด็ดขาด
.env
.env.local
.env.*.local
.dev.vars
.dev.vars.*
!.dev.vars.example
.clasp.json
.clasprc.json

# Supabase CLI เขียน project ref / pooler URL ของโปรเจกต์ที่ link ไว้ที่นี่
supabase/.temp/

# ข้อมูลจริงที่ dump ออกมาจากฐานข้อมูล ห้าม commit เด็ดขาด
backups/
*.sql.gz
backup-*.sql

# Test artifacts
coverage/
playwright-report/
test-results/

# Editor / OS
.DS_Store
*.log
*.tmp
*.bak
